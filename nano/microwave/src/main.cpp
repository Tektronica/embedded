#include <Arduino.h>

#include "Buzzer.h"
#include "KeyInput.h"
#include "Microwave.h"
#include "SevenSegment.h"

// Pin assignments, peripheral setup, and the main loop are pending hardware details (display
// interface, keypad wiring, motor/light switching) — see README's BOM and Status sections.

void setup() {}

void loop() {}
