[wokwi]
version = 1
# Simulate the firmware PlatformIO already builds for the arduino-nano env. The display isn't wired yet
# (see main.cpp's TODO), so this diagram covers the keypad, buzzer, and motor/fan/light outputs.
firmware = ".pio/build/arduino-nano/firmware.hex"
elf = ".pio/build/arduino-nano/firmware.elf"
